# Server-side Solana RPC. The browser talks to /api/rpc, which forwards here,
# so a paid Helius/Triton key never reaches the client.
RPC_URL=https://api.mainnet-beta.solana.com

# Port for the proxy + static server (production) or the API side of `npm run dev`.
PORT=8787
